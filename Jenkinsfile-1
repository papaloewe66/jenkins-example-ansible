node('MACOS') {
  stage('clean') {
    cleanWs()
  }
  stage('Hello') {
      dir('usrlocal/bin') {
        sh '''
          ansible --version
          ansible-playbook --version
          ansible-galaxy --version
        '''
      }
  }
}

  
