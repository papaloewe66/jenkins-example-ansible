node('MACOS') {
  stages {
      stage('clean') {
        cleanWs()
      }
      stage('Hello') {
        steps {
            sh '''
              ansible --version
              ansible-playbook --version
              ansible-galaxy --version
            '''
          }
      }
    }
}

  
